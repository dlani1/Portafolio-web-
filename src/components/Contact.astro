---

---
<section id="contacto" class="py-16 md:py-24">
  <div class="container mx-auto max-w-5xl px-4">

    <div class="max-w-xl mx-auto text-center mb-12">
      <h2 class="text-4xl md:text-5xl font-bold text-white mb-4">
        Hablemos
      </h2>
      <p class="text-lg text-gray-300">
        ¿Interesada en mi trabajo o tienes una pregunta? 
        Envíame un mensaje directo.
      </p>
    </div>

    <form 
      name="contact-dani" 
      method="POST" 
      data-netlify="true" 
      class="max-w-xl mx-auto text-left"
    >
      <input type="hidden" name="form-name" value="contact-dani" />

      <div class="mb-4">
        <label for="name" class="block text-gray-300 mb-2">Tu Nombre</label>
        <input 
          type="text" 
          name="name" 
          id="name" 
          required 
          class="w-full p-3 rounded-lg bg-brand-gray border border-brand-gray text-brand-light focus:outline-none focus:ring-2 focus:ring-brand-red"
        />
      </div>

      <div class="mb-4">
        <label for="email" class="block text-gray-300 mb-2">Tu Email</label>
        <input 
          type="email" 
          name="email" 
          id="email" 
          required 
          class="w-full p-3 rounded-lg bg-brand-gray border border-brand-gray text-brand-light focus:outline-none focus:ring-2 focus:ring-brand-red"
        />
      </div>

      <div class="mb-4">
        <label for="message" class="block text-gray-300 mb-2">Mensaje</label>
        <textarea 
          name="message" 
          id="message" 
          rows="4" 
          required 
          class="w-full p-3 rounded-lg bg-brand-gray border border-brand-gray text-brand-light focus:outline-none focus:ring-2 focus:ring-brand-red"
        ></textarea>
      </div>

      <div class="text-center">
        <button 
          type="submit" 
          class="bg-red-900 hover:bg-red-800 text-white font-bold py-3 px-8 rounded-lg transition-colors w-full sm:w-auto"
        >
          Enviar Mensaje
        </button>
      </div>
    </form>
    </div>
</section>